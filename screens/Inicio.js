import { Text, View } from "react-native"
import st from "../styles/Style"

export default Inicio = (props) => {
    return (
        <View style={[st.pt10,st.px2]} >
            <Text style={[st.textMd]}>Inicio</Text>
        </View>
    )
}